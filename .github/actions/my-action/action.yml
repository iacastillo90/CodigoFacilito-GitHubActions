name: "My Action"
description: "Test my action"

inputs:
  message:
    description: "The message to print"
    required: true
    default: "hola"

runs:
  using: "composite"

  steps: 
    - name: Hola Mundo
      uses: echo "Hola ${inputs.message}}"
      shell: bash